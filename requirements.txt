# Install everything with:  pip install -r requirements.txt
pandas
numpy
duckdb
scikit-learn
scipy
matplotlib
streamlit
# To run the .ipynb notebooks locally, also: pip install notebook
# (left out of this file so the Streamlit Cloud deploy stays lean)
